apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-data-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: default
data:
  INFLUX_URL: "http://influxdb:8086"
  INFLUX_TOKEN: "my-super-secret-auth-token"
  INFLUX_ORG: "fermentation"
  INFLUX_BUCKET: "sensors"
  HOME_ASSISTANT_URL: "http://192.168.1.51:8123"
  HOME_ASSISTANT_TOKEN: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiIyNWU0N2RkOWRmMzk0Zjc1YTcyMjliMjVlOTNjZTcyYiIsImlhdCI6MTc2NDU5NTIzNCwiZXhwIjoyMDc5OTU1MjM0fQ.ROlYsVmYOWxAYriPeOU0khl74W2jGwwaeNPRzNW0lQE"
  POLL_INTERVAL: "300000"
  DB_PATH: "/data/fermentation.db"
  ADMIN_PASSWORD: "ferment2024"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fermentation-backend
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fermentation-backend
  template:
    metadata:
      labels:
        app: fermentation-backend
      annotations:
        configmap/update: "2025-12-06T22:30:00Z"
    spec:
      containers:
      - name: backend
        image: ghcr.io/timbenedet/myfermentlab-backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3001
        envFrom:
        - configMapRef:
            name: backend-config
        volumeMounts:
        - name: backend-data
          mountPath: /data
      volumes:
      - name: backend-data
        persistentVolumeClaim:
          claimName: backend-data-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: fermentation-backend
  namespace: default
spec:
  selector:
    app: fermentation-backend
  ports:
  - port: 3001
    targetPort: 3001
  type: ClusterIP
