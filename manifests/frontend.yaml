apiVersion: apps/v1
kind: Deployment
metadata:
  name: fermentation-monitor
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fermentation-monitor
  template:
    metadata:
      labels:
        app: fermentation-monitor
    spec:
      containers:
      - name: frontend
        image: ghcr.io/timbenedet/myfermentlab-frontend:main-27f4c94
        imagePullPolicy: Always
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: fermentation-monitor
  namespace: default
spec:
  selector:
    app: fermentation-monitor
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  type: NodePort
